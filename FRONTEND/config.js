// Base URL for API calls.
// When served by Flask (python app.py), leave as empty string — routes are relative.
// When deploying frontend separately (Netlify, Vercel), set this to your backend URL:
//   const BASE_URL = "https://your-backend.onrender.com/api";
const BASE_URL = "/api";
